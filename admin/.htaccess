# The dashboard is served directly, never through the site's rewrite rules.
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>
Options -Indexes

# Uncomment and add your own IP to lock the dashboard to one location.
# <RequireAll>
#     Require ip 203.0.113.4
# </RequireAll>
